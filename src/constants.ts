export const EXIT_TILE_ID = "EXIT";
export const HINT_TILE_ID = "HINT";

export const GRID_HEIGHT = 3;
export const GRID_WIDTH = 8;
export const BANK_EXTRAS_ROW = Math.floor(GRID_HEIGHT / 2);

// Number of extra columns added to both left and right sides of bank
export const SIDE_PADDING = 2;
